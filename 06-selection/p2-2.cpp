#include <iostream>
#include<climits>

using namespace std;

int main() {
    int a, n, max = INT_MIN;

    cin >> n;

    if (n > 0) {
        cin >> a;
        if (a > max)
            max = a;
        n--;
    }
    if (n > 0) {
        cin >> a;
        if (a > max)
            max = a;
        n--;
    }
    if (n > 0) {
        cin >> a;
        if (a > max)
            max = a;
        n--;
    }
    if (n > 0) {
        cin >> a;
        if (a > max)
            max = a;
        n--;
    }
    if (n > 0) {
        cin >> a;
        if (a > max)
            max = a;
        n--;
    }
    if (n > 0) {
        cin >> a;
        if (a > max)
            max = a;
        n--;
    }
    if (n > 0) {
        cin >> a;
        if (a > max)
            max = a;
        n--;
    }
    if (n > 0) {
        cin >> a;
        if (a > max)
            max = a;
        n--;
    }
    if (n > 0) {
        cin >> a;
        if (a > max)
            max = a;
        n--;
    }
    if (n > 0) {
        cin >> a;
        if (a > max)
            max = a;
        n--;
    }

    cout << max << '\n';
    return 0;
}