#include <iostream>
#include<climits>

using namespace std;

int main() {
    int a, max = INT_MIN;

    cin >> a;
    if (a > max)
        max = a;
    cin >> a;
    if (a > max)
        max = a;
    cin >> a;
    if (a > max)
        max = a;
    cin >> a;
    if (a > max)
        max = a;
    cin >> a;
    if (a > max)
        max = a;
    cin >> a;
    if (a > max)
        max = a;
    cin >> a;
    if (a > max)
        max = a;
    cin >> a;
    if (a > max)
        max = a;
    cin >> a;
    if (a > max)
        max = a;
    cin >> a;
    if (a > max)
        max = a;
    cout << max << '\n';
    return 0;
}